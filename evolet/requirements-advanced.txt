# Optional advanced parser/extraction stack for doc-reader.
# Install on the GPU VM only when you want to enable the corresponding adapter.

surya-ocr>=0.8
paddleocr>=3.0
ultralytics>=8.3
python-doctr[torch]>=1.0
easyocr>=1.7
opencv-python-headless>=4.10
docling>=2.0
gliner>=0.2
medspacy>=1.3
scispacy>=0.5
datasets>=3.0
